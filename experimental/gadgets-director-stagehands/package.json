{
  "name": "@cloudflare/agents-gadgets-director-stagehands-example",
  "description": "Director and stagehands: the director selects focused stagehands at runtime and streams their work inline before synthesizing an answer",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "start": "vite dev",
    "deploy": "vite build && wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false"
  },
  "dependencies": {
    "@cloudflare/agents-ui": "*",
    "@cloudflare/ai-chat": "*",
    "@cloudflare/kumo": "^1.10.0",
    "@phosphor-icons/react": "^2.1.10",
    "@streamdown/code": "^1.1.0",
    "agents": "*",
    "streamdown": "^2.4.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4.2.1"
  }
}
